"""Replay harvesting: bank raw match data from public APIs over time.

The harvester is meant to run continuously on free API tiers so training data
accumulates for free (time, not money, is the cost). See harvest/daemon.py.
"""
